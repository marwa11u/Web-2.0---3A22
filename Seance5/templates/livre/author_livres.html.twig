<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>livres List</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f8ff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .table-container {
            background: #ffffff;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            max-width: 800px;
            margin: 40px auto;
            position: relative;
        }

        h1 {
            text-align: center;
            color: #007bff;
            margin-bottom: 20px;
            animation: text-wave 1s infinite alternate;
        }

        .livre-row {
            transition: transform 0.3s;
        }

        .livre-row:hover {
            transform: scale(1.02);
            background-color: #e9ecef;
        }

        .livre-row img {
            border-radius: 50%;
            transition: transform 0.3s;
        }

        .livre-row img:hover {
            transform: scale(1.05);
        }

        .text-hover {
            transition: color 0.3s;
        }

        .text-hover:hover {
            color: #007bff;
            /* Change text color on hover */
        }

        .back-link {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 20px;
            border-radius: 30px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .back-link:hover {
            background-color: #0056b3;
        }

        @keyframes text-wave {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-5px);
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="table-container">
            <h1>livres List</h1>

            <div class="text-center mb-4">
                <a href="{{ path('app_add') }}" class="btn btn-primary btn-lg">Add livre</a>
            </div>

            {% if livres is defined and livres|length > 0 %}
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>ref</th>
                        <th>title</th>
                        <th>nbrPage</th>
                        <th>author</th>
                        <th>picture</th>
                    </tr>
                </thead>
                <tbody>
                    {% for livre in livres %}
                    <tr class="livre-row">
                        <td>
                            <h5 class="text-hover">{{ livre.ref }}</h5>
                        </td>
                        <td>
                            <h5 class="text-hover">{{ livre.title }}</h5>
                        </td>
                        <td>
                            <h5 class="text-hover">{{ livre.nbrPage }}</h5>
                        </td>
                        <td>
                            <h5 class="text-hover">{{ livre.Author }}</h5>
                        </td>
                        <td>
                            <h5 class="text-hover">{{ livre.picture }}</h5>
                        </td>
                        <td>
                            <a href="{{ path('app_edit', {'id': livre.id}) }}" class="btn btn-warning btn-sm">Edit</a>
                            <a href="{{ path('app_delete', {'id': livre.id}) }}" class="btn btn-danger btn-sm"
                                onclick="return confirm('Are you sure you want to delete this livre?');">Delete</a>
                        </td>
                    </tr>
                    {% endfor %}

                </tbody>
            </table>
            {% else %}
            <p class="text-center">There is no livre available.</p>
            {% endif %}
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>