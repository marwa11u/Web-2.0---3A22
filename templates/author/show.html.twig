<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Details</title>
   
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f8ff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .author-details {
            background: #ffffff;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            margin: 40px auto;
            text-align: center;
            transition: transform 0.3s ease;
        }
        .author-details img {
            border-radius: 50%;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
            width: 150px;
            height: 150px;
            transition: transform 0.3s ease;
        }
        .author-details img:hover {
            transform: scale(1.1); 
        }
        h4 {
            font-weight: 700;
            color: #343a40;
        }
        p {
            color: #6c757d;
            font-size: 1.1rem;
        }
        .back-link {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 20px;
            border-radius: 30px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }
        .back-link:hover {
            background-color: #0056b3;
        }
        .text-hover {
            transition: color 0.3s;
        }
        .text-hover:hover {
            color: #007bff;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="author-details">
            <strong><h4> Details </h4></strong>
            {% for author in authors %}
                {% if id == author.id %}
                    <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" class="img-fluid">
                    <p><strong>Name:</strong> <span class="text-hover">{{ author.username }}</span></p>
                    <p><strong>Email:</strong> <span class="text-hover">{{ author.email }}</span></p>
                    <p><strong>Number of Books:</strong> <span class="text-hover">{{ author.nb_books }}</span></p>
                    <a href="{{ path('list_authors') }}" class="back-link">Back to Author List</a>
                {% endif %}
            {% endfor %}
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
